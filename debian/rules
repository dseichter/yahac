#!/usr/bin/make -f

export DH_VERBOSE=1
export PYBUILD_NAME=yahac

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_install:
	dh_auto_install
	install -D -m 755 src/yahac.py debian/yahac/usr/bin/yahac
